# --- Core ---
python-dotenv==1.0.1
pyyaml==6.0.2
sqlalchemy==2.0.35

# --- Collectors (Day 2) ---
requests==2.32.3
feedparser==6.0.11

# --- Processing: embeddings for dedup/classification (Day 3) ---
sentence-transformers==3.1.1
chromadb==0.5.15

# --- MCP server (Day 5) ---
mcp==1.1.2
fastapi==0.115.5
uvicorn==0.32.1
pydantic==2.9.2

# Note: sentence-transformers pulls in torch, which is a large install.
# It's included now so `pip install -r requirements.txt` only needs to run
# once, but Day 1-2 work doesn't actually need it yet.
